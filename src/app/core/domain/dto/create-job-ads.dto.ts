export interface CreateJobAdsDto {
  title?: string
  description?: string
  requirements?: string
  salary?: number
  workType?: string
  payType?: string
  location?: string
}

// TODO: Remove ? later

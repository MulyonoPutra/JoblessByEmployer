export interface CreateJobAdsDto {
    title?: string;
    description?: string;
    requirements?: string;
    salary?: string;
    workType?: string;
    payType?: string;
    location?: string;
}

// TODO: Remove ? later
